import { Box, Text } from '@components'

export const Interval = () => (
  <Box
    flex={1}
    width="100%"
    position="absolute"
    top={0}
    pt={12}
    alignItems="center"
  >
    <Text fontWeight="bold" fontSize={120}>
      +
    </Text>
  </Box>
)
